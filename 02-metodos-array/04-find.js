const alunos = ["Julia", "Sofia", "Pablo", "larissa"];

const aluno = alunos.find((nome) =>  nome === "Sofia" );

console.log(aluno);

const aluno1 = alunos.find((nome) => nome === "Isabela");
console.log(aluno1);
